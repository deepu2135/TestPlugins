name: Build Extensions

on:
  push:
    branches:
      - master
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Grant execute permission
        run: chmod +x gradlew

      - name: Build Providers
        run: ./gradlew :GramCinemaProvider:make :TelegramProvider:make

      - name: Generate repo.json and plugins.json
        run: |
          mkdir -p builds

          echo '{
            "name": "Deepu Repo",
            "iconUrl": "https://bollywood.eu.org/favicon.ico",
            "description": "Gram Cinema and Telegram Extensions",
            "manifestVersion": 1,
            "pluginLists": [
              "https://raw.githubusercontent.com/deepu2135/TestPlugins/refs/heads/builds/plugins.json"
            ]
          }' > builds/repo.json

          echo '[
            {
              "name": "Gram Cinema",
              "internalName": "GramCinemaProvider",
              "url": "https://raw.githubusercontent.com/deepu2135/TestPlugins/refs/heads/builds/GramCinemaProvider.cs3",
              "tvTypes": ["Movie", "TvSeries"],
              "language": "hi",
              "iconUrl": "https://bollywood.eu.org/favicon.ico",
              "apiVersion": 10,
              "version": 1,
              "authors": ["deepu2135"],
              "status": 1,
              "description": "Bollywood, Hollywood, Tamil & Telugu from Gram Cinema"
            },
            {
              "name": "Telegram",
              "internalName": "TelegramProvider",
              "url": "https://raw.githubusercontent.com/deepu2135/TestPlugins/refs/heads/builds/TelegramProvider.cs3",
              "tvTypes": ["Movie"],
              "language": "en",
              "iconUrl": "https://upload.wikimedia.org/wikipedia/commons/8/82/Telegram_logo.svg",
              "apiVersion": 10,
              "version": 1,
              "authors": ["Antigravity"],
              "status": 1,
              "description": "Stream any media from your Telegram chats and channels on-demand"
            }
          ]' > builds/plugins.json

      - name: Push to builds branch
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git fetch origin
          git checkout -B builds
          cp -r builds/. .
          git add -f repo.json plugins.json *.cs3 || true
          git status
          git commit -m "Update builds" || true
          git push origin builds --force

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: GramCinemaProvider
          path: builds/
